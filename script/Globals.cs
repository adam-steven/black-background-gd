using Godot;

public static class Globals {
    public static Vector2 levelSize = new Vector2(460, 460); //spawnable level radius (half of full dimentions)
    public static string scenePath = "/root/Scene";
}